#ifndef ACID_TESTS_DLG_H
#define ACID_TESTS_DLG_H

// Emulation > Acid Tests: modal GB Emulator Shootout runner. Destroys the
// current GB session; the caller reloads the user's ROM after it returns.
void WinShowAcidTestsDialog();

#endif
